{
  "name": "fast-hostel-web",
  "version": "1.0.0",
  "scripts": {
    "build": "if [ ! -d \"flutter_sdk\" ]; then git clone https://github.com/flutter/flutter.git -b stable flutter_sdk; fi && ./flutter_sdk/bin/flutter build web --release --no-tree-shake-icons"
  }
}